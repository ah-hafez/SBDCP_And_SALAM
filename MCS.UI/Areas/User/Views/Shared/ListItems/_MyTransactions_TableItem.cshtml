@using MCS.UI.Helpers
@using MCS.Common
@using MCS.UI.Areas.User.Models.File
@model TransactionTrayInfoVM
@{
    bool isPermition = false;
    string Confidentiality = "ConfidentialityNormal";
    string ConfidentialityBac = "ConfidentialityNormalBackg";
    // TransactionCategories

    string transType = string.Empty;

    if (Model.TransactionDetailsInfoVM.TransactionCategoryId == (int)TransactionCategorieColor.InternalOutbound)
    {

        transType = "InternalOutboundLineTable";

    }
    else if (Model.TransactionDetailsInfoVM.TransactionCategoryId == (int)TransactionCategorieColor.Inbound)
    {

        transType = "InboundLineTable";


    }
    else
    {
        transType = "OutboundExternalTable";
    }

    switch (Model.TransactionDetailsInfoVM.ConfidentialityId)
    {
        case (int)Confedentiality.HandDelivered:
            {
                Confidentiality = "ConfidentialityVeryRed";
                ConfidentialityBac = "ConfidentialityVeryRedBackg";

                if (SessionInfo.CurrentUser.Claims.Contains(UserClaims.ConfidentialityOfTransactions.HandDelivered))
                {
                    isPermition = true;
                }

                break;
            }
        case (int)Confedentiality.HighConfidential:
            {
                Confidentiality = "ConfidentialityMidRed";
                ConfidentialityBac = "ConfidentialityMidRedBackg";

                if (SessionInfo.CurrentUser.Claims.Contains(UserClaims.ConfidentialityOfTransactions.HandDelivered) || SessionInfo.CurrentUser.Claims.Contains(UserClaims.ConfidentialityOfTransactions.ExtremlyConfidential))
                {
                    isPermition = true;
                }

                break;
            }
        case (int)Confedentiality.Secret:
            {
                Confidentiality = "ConfidentialityRed";
                ConfidentialityBac = "ConfidentialityRedBackg";

                if (SessionInfo.CurrentUser.Claims.Contains(UserClaims.ConfidentialityOfTransactions.HandDelivered) || SessionInfo.CurrentUser.Claims.Contains(UserClaims.ConfidentialityOfTransactions.ExtremlyConfidential) || SessionInfo.CurrentUser.Claims.Contains(UserClaims.ConfidentialityOfTransactions.Secret))
                {
                    isPermition = true;
                }

                break;
            }
        case (int)Confedentiality.Normal:
            {
                isPermition = true;
                break;
            }
    }

    bool isViewed = false;
    string isViewedHeaderClass = "correspondence-t";
    if (Model.TransactionAssignmentInfoVMs != null && Model.TransactionAssignmentInfoVMs.Count > 0)
    {
        isViewed = Model.TransactionAssignmentInfoVMs.FirstOrDefault().Viewed;
    }
    isViewedHeaderClass = isViewed ? "correspondence-t" : "correspondence-t correspondence-t-notviewed bold ";

    bool HasPermissionToOpenEncrypted = true;
    if (Model.TransactionDetailsInfoVM.Encrypted)
    {
        if (SessionInfo.CurrentUser.Claims.Contains(UserClaims.GeneralPermissions.OpenEncryptTransaction))
        {
            HasPermissionToOpenEncrypted = true;
        }
        else
        {

            HasPermissionToOpenEncrypted = false;
        }

    }
}



<tr class="bg-white @Confidentiality @isViewedHeaderClass @transType" style="cursor: pointer; " data-id="@Model.TransactionDetailsInfoVM.Id">
    <td>
        <div class="checkbox">
            @if (HasPermissionToOpenEncrypted)
            {
                if (Model.TransactionDetailsInfoVM.HasPermission)
                {
                    <label>
                        <input type="checkbox" value="" onchange="holdDataFromBox(this); EnablePrint(this);" id="ch_@Model.TransactionDetailsInfoVM.Id" data-id="@Model.TransactionDetailsInfoVM.Id" data-encryptedid="@Model.TransactionDetailsInfoVM.EncryptedId" class="FillIds">
                        <span class="cr"><i class="cr-icon glyphicon glyphicon-ok"></i></span>
                    </label>
                }
            }
            </div>

    </td>
    @*<td>
            @if (!isViewed)
            {
                <div class="badgeNew">
                    <span>@DbRes.TResource("User.Shared.UnRead")</span>
                </div>
            }
        </td>*@
    <td>
        <div class="pr-1" id="Number_@Model.TransactionDetailsInfoVM.Id">@Model.TransactionDetailsInfoVM.Number</div>

    </td>

    @*<td @ViewData["onClick"] onclick="redirect()">
            @Model.TransactionDetailsInfoVM.TransactionCategory
        </td>*@
    <td class="title" @*id="entityName_5306"*@ @ViewData["onClick"] onclick="redirect()">
        @if (Model.TransactionDetailsInfoVM.TransactionCategoryId != TransactionCategory.InternalOutbound.LookupIdentity(LookupCategory.TransactionCategory, SessionInfo.CultureShortName))
        {
            if (Model.TransactionDetailsInfoVM.ExternalPartyName == null)
            {
                <div></div>
            }
            else
            {
                <div class="title" id="entityName_@Model.TransactionDetailsInfoVM.Id">@Model.TransactionDetailsInfoVM.ExternalPartyName</div>
                <div>
                    @*@(Model.TransactionAssignmentInfoVMs[0].FromUser.CompareTo(Model.TransactionAssignmentInfoVMs[0].ToUser) == 0 ? "" : Model.TransactionAssignmentInfoVMs[0].FromUser)*@
                </div>
            }

        }

        else
        {
            <div class="title" id="entityName_@Model.TransactionDetailsInfoVM.Id" onclick="redirect()">
                @Model.TransactionDetailsInfoVM.EntityName
            </div>
            <div class="">
                @*@(Model.TransactionAssignmentInfoVMs[0].FromUser.CompareTo(Model.TransactionAssignmentInfoVMs[0].ToUser) == 0 ? "" : Model.TransactionAssignmentInfoVMs[0].FromUser)*@
            </div>
        }
    </td>


    <td class="subject-td" @ViewData["onClick"] onclick="redirect()">
        @if (HasPermissionToOpenEncrypted)
        {
            if (Model.TransactionDetailsInfoVM.HasPermission)
            {
                <div id="example" class="Tabledescription">
                    @Model.TransactionDetailsInfoVM.Subject
                </div>
            }
            else
            {
                <div class="description">***</div>
            }
        }
        else
        { <div class="description">@DbRes.TResource("User.File.EncryptedTransaction")</div>
           

            
        }
    </td>
    <td @ViewData["onClick"] onclick="redirect()">
        <div class="" style="text-align: right;">
            @DateHelper.GetDate(@Model.TransactionDetailsInfoVM.DateH)
            @Model.TransactionDetailsInfoVM.Date.ToShortTimeString()
        </div>
    </td>
    <td @ViewData["onClick"] onclick="redirect()">
        <div class="result">
            @if (Model.TransactionDetailsInfoVM.PriorityName != null && Model.TransactionDetailsInfoVM.PriorityName.Length > 15)
            {
                <div class="" data-bs-toggle="tooltip" data-placement="bottom" title="@Model.TransactionDetailsInfoVM.PriorityName">
                    @Model.TransactionDetailsInfoVM.PriorityName
                </div>
            }
            else
            {
                <div class="">
                    @Model.TransactionDetailsInfoVM.PriorityName
                </div>
            }
        </div>
    </td>
    <td @ViewData["onClick"] onclick="redirect()">
        @if (Model.TransactionDetailsInfoVM.ConfidentialityName != null && Model.TransactionDetailsInfoVM.ConfidentialityName.Length > 10)

        {
            <div class="@ViewData["className"]" data-bs-toggle="tooltip" data-placement="bottom" title="@Model.TransactionDetailsInfoVM.ConfidentialityName">
                @Model.TransactionDetailsInfoVM.ConfidentialityName.Substring(0, 10)...
            </div>
        }
        else
        {
            <div class="@ViewData["className"]">
                @Model.TransactionDetailsInfoVM.ConfidentialityName
            </div>
        }
    </td>


    @*<td @ViewData["onClick"]>
            @if (Model.TransactionAssignmentInfoVMs != null && Model.TransactionAssignmentInfoVMs.Count > 0)
            {
                <div class="">

                    @Model.TransactionAssignmentInfoVMs.FirstOrDefault().Action
                    @if (Model.TransactionAssignmentInfoVMs.FirstOrDefault().Action == null)
                    {
                        <div class="">
                            @DbRes.TResource("Admin.AssignmentPaperActions.ActionUnderProcedure")
                        </div>
                    }

                </div>
            }
            else
            {
                <div class="">
                    @DbRes.TResource("Admin.AssignmentPaperActions.ActionUnderProcedure")
                </div>
            }
        </td>*@

    @*<td @ViewData["onClick"]>
            @if (Model.TransactionAssignmentInfoVMs.FirstOrDefault().Date != null && Model.TransactionAssignmentInfoVMs.FirstOrDefault().Action != null)
            {
                <div class="" data-bs-toggle="tooltip" data-placement="bottom" title="@Model.TransactionAssignmentInfoVMs.FirstOrDefault().Date">
                    @if (Model.TransactionAssignmentInfoVMs.FirstOrDefault().Date.ToString().Length > 10)
                    {
                        @Model.TransactionAssignmentInfoVMs.FirstOrDefault().Date.ToString().Substring(0, 10)
                    }
                    else
                    {
                        @Model.TransactionAssignmentInfoVMs.FirstOrDefault().Date.ToString()
                    }

                </div>
            }
            else
            {
                <div class="">
                    ---
                </div>
            }
        </td>*@

    <td @ViewData["onClick"]>
        @if (Model.TransactionAssignmentInfoVMs.FirstOrDefault().FromEntity != null && Model.TransactionAssignmentInfoVMs.FirstOrDefault().Action != null)
        {
            <div class="subject-td" data-bs-toggle="tooltip" data-placement="bottom" title="@Model.TransactionAssignmentInfoVMs.FirstOrDefault().FromEntity">


                @Model.TransactionAssignmentInfoVMs.FirstOrDefault().FromEntity

            </div>
        }
        else
        {
            <div class="">
                ---
            </div>
        }
    </td>
    @*<td @ViewData["onClick"]>
            @if (Model.TransactionAssignmentInfoVMs.FirstOrDefault().FromUser != null && Model.TransactionAssignmentInfoVMs.FirstOrDefault().Action != null)
            {
                <div class="" data-bs-toggle="tooltip" data-placement="bottom" title="@Model.TransactionAssignmentInfoVMs.FirstOrDefault().FromUser">
                    @if (Model.TransactionAssignmentInfoVMs.FirstOrDefault().FromUser.Length > 15)
                    {
                        @Model.TransactionAssignmentInfoVMs.FirstOrDefault().FromUser.Substring(0, 10)
                    }
                    else
                    {
                        @Model.TransactionAssignmentInfoVMs.FirstOrDefault().FromUser
                    }


                </div>
            }
            else
            {
                <div class="">

                    ---


                </div>
            }
        </td>

        <td>
            @Model.TransactionDetailsInfoVM.AttachmentCount
        </td>*@

    <td class="">
        <div class="correspondence-icon-table d-flex">
            @if (HasPermissionToOpenEncrypted)
            {
                if ((TransactionCategory)Model.TransactionDetailsInfoVM.TransactionCategoryId == (TransactionCategory)TransactionCategory.Inbound.LookupIdentity(LookupCategory.TransactionCategory, SessionInfo.CultureShortName))
                {
                    if (SessionInfo.CurrentUser.Claims.Contains(UserClaims.Inbound.EditInbound) || Model.IsVIPUser)
                    {
                        @*if (!Model.IsVIPUser)
                            {
                                <a id="Certificate" href="#" title="@DbRes.TResource("User.InboundSearch.InboundCertificate")"   onclick="InboundCertificate(@Model.TransactionDetailsInfoVM.Id)">
                                    <img src="~/Content/User/lib/images/SystemIcon/main bar-14.svg" width="30" />
                                </a>
                            }*@


                        <div>
                            <a title="@DbRes.TResource("User.Inbound.EditInbound")" data-Viewed="@ViewData["isViewed"]"
                               data-DeliveryMethodId="@Model.TransactionDetailsInfoVM.DeliveryMethodId"
                               onclick="Open(@Model.TransactionDetailsInfoVM.Id,'@Model.TransactionDetailsInfoVM.EncryptedId')">
                                @if (!Model.IsVIPUser)
                                {
                                    <img src="~/Content/User/lib/images/SystemIcon/main bar-24.svg" width="30" />
                                }
                                else
                                {
                                    <img src="~/Content/User/lib/images/SystemIcon/main bar-22.svg" width="30" />
                                }
                            </a>
                        </div>

                    }
                    if (!Model.IsVIPUser)
                    {
                        <div>
                            <a id="OpenEditor" title="المحرر" data-Viewed="@ViewData["isViewed"]"
                               data-DeliveryMethodId="@Model.TransactionDetailsInfoVM.DeliveryMethodId"
                               onclick="OpenEditors(@Model.TransactionDetailsInfoVM.Id,'@Model.TransactionDetailsInfoVM.EncryptedId')">
                                <img src="~/Content/User/lib/images/SystemIcon/main bar-22.svg" width="30" />
                            </a>
                        </div>

                    }
                    if (Model.IsVIPUser)
                    {
                        <div>
                            <a title="@DbRes.TResource("Global.Return")"
                               onclick="javascript:showRejectionPopup(@Model.TransactionDetailsInfoVM.Id)">
                                <img src="~/Content/User/lib/images/SystemIcon/main bar-36.svg" width="25" />
                            </a>
                        </div>
                    }
                    @*if (SessionInfo.CurrentUser.Claims.Contains(UserClaims.Outbound.CreateExternalOutbound) && !Model.IsVIPUser)
                        {
                            <div>
                                <a data-id="@Model.TransactionDetailsInfoVM.Id"
                                   onclick="CreateOutboundDraft('@Model.TransactionDetailsInfoVM.EncryptedId')" title="@DbRes.TResource("User.File.CreateOutbound")">
                                    <img src="~/Content/User/lib/images/SystemIcon/add.svg" width="30" />
                                </a>
                            </div>

                    }*@
                    }
                else if ((TransactionCategory)Model.TransactionDetailsInfoVM.TransactionCategoryId == (TransactionCategory)TransactionCategory.InternalOutbound.LookupIdentity(LookupCategory.TransactionCategory, SessionInfo.CultureShortName))
                    {
                        if (SessionInfo.CurrentUser.Claims.Contains(UserClaims.Outbound.EditInternalOutbound) || Model.IsVIPUser)
                        {
                            @*if (!Model.IsVIPUser)
                        {

                                <a id="Certificate" href="#" title="@DbRes.TResource("User.InboundSearch.InboundCertificate")"   onclick="InboundCertificate(@Model.TransactionDetailsInfoVM.Id)">
                                    <img src="~/Content/User/lib/images/SystemIcon/main bar-14.svg" width="30" />
                                </a>

                        }*@

                        <div>
                            <a title=" @DbRes.TResource("User.OutboundInternal.EditOutboundInternal")" data-Viewed="@ViewData["isViewed"]"
                               data-DeliveryMethodId="@Model.TransactionDetailsInfoVM.DeliveryMethodId"
                               onclick="Open(@Model.TransactionDetailsInfoVM.Id,'@Model.TransactionDetailsInfoVM.EncryptedId')">
                                @if (!Model.IsVIPUser)
                                {
                                    <img src="~/Content/User/lib/images/SystemIcon/main bar-24.svg" width="30" />
                                }
                                else
                                {
                                    <img src="~/Content/User/lib/images/SystemIcon/main bar-22.svg" width="30" />
                                }
                            </a>
                        </div>

                        }
                        if (!Model.IsVIPUser)
                        {

                        <div>
                            <a id="OpenEditor" title="المحرر" data-Viewed="@ViewData["isViewed"]"
                               data-DeliveryMethodId="@Model.TransactionDetailsInfoVM.DeliveryMethodId"
                               onclick="OpenEditors(@Model.TransactionDetailsInfoVM.Id,'@Model.TransactionDetailsInfoVM.EncryptedId')">
                                <img src="~/Content/User/lib/images/SystemIcon/main bar-22.svg" width="30" />
                            </a>
                        </div>

                    }
                    if (Model.IsVIPUser)
                    {
                        <div>
                            <a title="@DbRes.TResource("Global.Return")"
                               onclick="javascript:showRejectionPopup(@Model.TransactionDetailsInfoVM.Id)">
                                <img src="~/Content/User/lib/images/SystemIcon/main bar-36.svg" width="25" />
                            </a>
                        </div>
                    }

                        @*if (!Model.IsVIPUser)
                    {


                        <div>
                            <a id="OpenEditor" class="circle color-blue" title="المحرر" data-Viewed="@ViewData["isViewed"]"
                               data-DeliveryMethodId="@Model.TransactionDetailsInfoVM.DeliveryMethodId"
                               onclick="OpenEditors(@Model.TransactionDetailsInfoVM.Id,'@Model.TransactionDetailsInfoVM.EncryptedId')">
                                <i class="icon-outline-subtitles"></i>
                            </a>
                        </div>
                    }*@

                }
            }
           
            
            else
            {
                <div>
                    <a title="@DbRes.TResource("Global.Return")"
                       onclick="javascript:showRejectionPopup(@Model.TransactionDetailsInfoVM.Id)">
                        <img src="~/Content/User/lib/images/SystemIcon/undo.svg" width="25" />
                    </a>
                </div>
            }
            @Html.StickyHidden("transId")
            @*<div class="ml-auto">
            <div class="info">
                <i class="la la-paperclip"></i>
                <span>
                    <span>
                        @Model.TransactionDetailsInfoVM.AttachmentCount
                    </span>
                    @DbRes.TResource("User.Transaction.Attachments")
                </span>
                @if (Model.TransactionDetailsInfoVM.HasLinks)
                {
                    <i class="la la-link"></i>
                    <span>
                        @DbRes.TResource("Admin.TransactionLink.Link")
                    </span>
                }
            </div>
        </div>*@
        </div>
    </td>
</tr>
@Html.StickyHidden("hdnEncrypted", Model.TransactionDetailsInfoVM.Encrypted.ToString(), new { id = "hdnEncrypted_" + Model.TransactionDetailsInfoVM.Id })
@Html.StickyHidden("TransactionType", ((TransactionCategory)Model.TransactionDetailsInfoVM.TransactionCategoryId.LookupInternalID(LookupCategory.TransactionCategory, SessionInfo.CultureShortName)).ToString(), new { id = "hdnTransactionType_" + Model.TransactionDetailsInfoVM.Id })
@Html.StickyHidden("Type", Model.TransactionDetailsInfoVM.TransactionCategoryId.ToString(), new { id = "hdnType_" + Model.TransactionDetailsInfoVM.Id })
<style>
    .ConfidentialityNormal {
    }

    .ConfidentialityRed {
        border-right: 4px solid #c41c0e;
    }

    .ConfidentialityMidRed {
        border-right: 4px solid #c41c0e;
    }

    .ConfidentialityVeryRed {
        border-right: 4px solid #c41c0e;
    }
</style>
<script>
     function redirect() {
        @if (!Model.IsVIPUser)
        {
            @:$(event.currentTarget).parent().find('#OpenEditor').click();
        }
        else
        {
            @:$(event.currentTarget).parent().find('#OpenVipEditor').click();
        }
    }



</script>
